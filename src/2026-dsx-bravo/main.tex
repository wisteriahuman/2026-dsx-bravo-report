%\documentclass[11pt]{jarticle}
\documentclass[11pt]{ujarticle} %%uplatexを用いる際はこちらを使用
\usepackage{fundsx}
\usepackage{url}
\usepackage[dvipdfmx]{graphicx}

\title{データサイエンス演習 最終レポート\\＜レポートのタイトル＞}

\author{% 
＜グループ番号・グループ名＞\\
＜学籍番号・名前（姓 名）＞\\
＜学籍番号・名前（姓 名）＞\\
＜学籍番号・名前（姓 名）＞\\
＜学籍番号・名前（姓 名）＞
}
\date{提出日 ＜提出日＞}

\begin{document}

\maketitle
\clearpage

\section{概要}
＜本レポートの概要を記述する．記述例：本グループではxxxを目的として，（xxxに着目し）xxxという仮説を立て検証した．検証にはxxx手法を用い，xxxデータセットによりモデルの訓練を行い，テストデータにて検証した結果，xxxであった．＞

\section{背景・⽬的}
＜ここに社会問題や社会背景，それによって導かれた目的や着目した要因，他の関連研究や他に検討した内容等を記述する．＞

% アウトライン（Issue #1）で追加した章．本文は Issue #3 の担当者が記述する．
\section{関連研究}
＜心理的モメンタムに関する先行研究（Redwood-Brown et al. 2018／Den Hartigh et al. 2019）と，本研究の位置づけを記述する．Issue \#3 参照．＞

\section{対象としたデータセット}
\label{sec:dataset}

\subsection{出自}

% fundsx.sty の脚注定義と url パッケージの相性が悪く，\footnote{\url{...}} はビルドが通らない．
% テンプレートの指示どおり，URLは本文中に置く．
本研究では，StatsBomb社が無償で公開しているイベントデータ
（StatsBomb Open Data，\url{https://github.com/statsbomb/open-data}）を用いた．
このデータは同社のアナリストが試合映像を見ながら手作業で記録したもので，
研究・教育目的での利用が認められている．
対象としたのは，このうちスペイン1部リーグ（ラ・リーガ）2015-16シーズンの全380試合である．

\subsection{データの内容と規模}

イベントデータとは，試合中に起きたプレーを1件ずつ時系列に並べて記録したものである．
パス，ボール運び，シュート，プレッシャーといった個々のプレーが1行に対応し，
それぞれについて発生時刻・実行したチームと選手・ピッチ上の位置などが記録されている．

全380試合に含まれるイベントは合計1{,}295{,}354件であり，
1試合あたりの中央値は3{,}375件（最小2{,}614件，最大4{,}326件）である．
イベントの種類は33通りあり，件数の多い順にパス365{,}288件，
ボール受け（Ball Receipt*）328{,}953件，ボール運び276{,}171件，
プレッシャー114{,}081件と続く．
この380試合で記録された得点は1{,}043点（うちオウンゴール29点）である．

このデータの特徴は，プレーが起きた\textbf{位置と時刻まで}記録されている点にある．
一方で，ボールに関与していない選手の位置は連続的には記録されていない．
すなわち，本データは選手全員の動きを追うトラッキングデータではなく，
あくまで「起きたプレーの記録」である．この点は本研究の限界にも直結する．

\subsection{データを構成する属性}

各イベントには，すべてのイベントに共通する属性と，
イベントの種類ごとに固有の属性が記録されている．
種類が該当しない属性は欠測となる（たとえばシュート固有の属性はパスの行には入らない）．
主な属性を表\ref{tab:attributes}に示す．

位置はピッチを120$\times$80ヤードに正規化した座標で与えられる．
座標は\textbf{各チームが自陣から相手ゴールへ向かって攻める向き}に統一されており，
同じ場面でも記録するチームによって値が反転する．
本研究では常に対象チームの視点で特徴量を計算しているため，この向きの違いは問題にならない．

\begin{table}[htbp]
    \centering
    \caption{イベントデータの主な属性．「例」の列は，図\ref{fig:dataset}に示した得点シュートに記録されていた値である．}
    \label{tab:attributes}
    \small
    \begin{tabular}{|l|p{6.2cm}|l|}
    \hline
    属性 & 内容 & 例 \\
    \hline
    \multicolumn{3}{|l|}{\textit{すべてのイベントに共通}} \\
    \hline
    \texttt{match\_id} & 試合ID & 266149 \\
    \hline
    \texttt{period} & 前半・後半の別 & 1（前半） \\
    \hline
    \texttt{minute}, \texttt{second} & 発生時刻 & 30分23秒 \\
    \hline
    \texttt{type} & イベントの種類（33種） & Shot \\
    \hline
    \texttt{team}, \texttt{player} & 実行したチームと選手 & Barcelona，Neymar \\
    \hline
    \texttt{location} & 発生位置（$x$, $y$） & (106.8, 29.8) \\
    \hline
    \texttt{duration} & 継続時間（秒） & 1.77 \\
    \hline
    \texttt{possession} & 一続きの攻撃の通し番号 & 46 \\
    \hline
    \texttt{under\_pressure} & 相手の圧力下でのプレーか & 真 \\
    \hline
    \multicolumn{3}{|l|}{\textit{イベントの種類ごとに固有}} \\
    \hline
    \texttt{shot\_statsbomb\_xg} & シュートの得点期待値（xG） & 0.147 \\
    \hline
    \texttt{shot\_outcome} & シュートの結果 & Goal \\
    \hline
    \texttt{pass\_end\_location} & パスの到達位置 & 欠測 \\
    \hline
    \end{tabular}
\end{table}

\subsection{記録の例}

実際にどのように記録されているかを図\ref{fig:dataset}に示す．
2016年1月17日のバルセロナ対アスレティック・ビルバオ戦から，
前半30分23秒の得点までの一続きの攻撃を，記録された位置のとおりに描いたものである．
自陣中央のパスから始まり，中盤でいったん相手にブロックされたボールを拾い直し，
前方へ運んでペナルティエリアへ通し，シュートに至るまでが約10秒間である．
この間に両チーム合わせて17件のイベントが記録されており，
図にはそのうち位置の移動を伴うバルセロナの7件を示した．

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/fig_dataset.png}
    \caption{イベントデータの記録例．2016年1月17日 バルセロナ対アスレティック・ビルバオ戦の前半30分23秒の得点までの一続きの攻撃（\texttt{possession}~46）を，記録された位置のとおりに描いた．白丸はイベントの発生位置，矢印は到達位置を表す．四角は相手にブロックされたボールを拾い直した位置である．受け手の記録（Ball Receipt*）など，位置の移動を伴わないイベントは省いている．}
    \label{fig:dataset}
\end{figure}

\subsection{分析対象の絞り込み}

380試合に記録された1{,}043点のうち，本研究が分析対象としたのは
\textbf{他のゴールと近接しない「隔離された失点」609件}である．
失点の前後の区間に別のゴールが入ってしまうと，
観察された変化がどちらの得点によるものか切り分けられないためである．
この609件は313試合に分布している．

\subsection{実プレー時間}

サッカーの試合時計は，ボールが外に出ている間も選手が倒れている間も進み続ける．
そのため，時計上の「5分間」に含まれる実際のプレー量は場面によって大きく異なる．
特に得点の直後は，喜びの輪や再開の準備でプレーが止まる時間が長くなりやすい．

そこで本研究では，時計ではなく\textbf{実プレー時間}を用いた．
これは，イベントに記録された継続時間（\texttt{duration}）を一続きの攻撃ごとにつなぎ合わせ，
10秒を超える空白をプレーの停止とみなして除いたものである．
この方法で復元した実プレー時間は，1試合あたりの中央値が52.0分
（四分位範囲 49.0〜54.9分）であった．
以降，本レポートで「3分」と述べるのはすべてこの実プレー時間である．

% アウトライン（Issue #1）で追加した章．本文は Issue #5 の担当者が記述する．
\section{中間発表までの取り組みと課題}
＜中間発表時のプレッシャー回数の比較と，その後に判明したキックオフ再開の構造による判別の押し上げ（AUC 0.703 の正体）までを記述する．Issue \#5 参照．＞

\section{⼿法}
\label{sec:method}

\subsection{「流れが変わる」を判別問題に置き換える}

「流れが変わった」かどうかを人が目で判定することはできない．そこで本研究では，この問いを
\textbf{失点の前後で起きたプレーぶりの変化を，よく似た別の場面で起きた変化と機械学習が見分けられるか}
という判別問題に置き換えた．
見分けられるならば，失点の前後には比較対象の場面にはない変化が起きたことになる．
逆に見分けられないならば，少なくとも本研究が用いた指標の範囲では，
失点に固有の変化を検出できなかったことになる．
判別の成績はAUC（Area Under the ROC Curve）で評価した．
AUCは1.0であれば完全に見分けられ，0.5であればまったく見分けられないことを意味する．

\subsection{プレーぶりを表す32項目}

「流れ」を一つの指標で代表させると，どの指標を選んだかによって結論が変わってしまう．
そこで本研究では，イベントデータから機械的に計算できる項目を選り好みせずに並べ，
実プレー3分間のプレーぶりを表\ref{tab:features}に示す32項目の数値ベクトルで表した．
内訳は，パスや守備の内容と位置を表す16項目，
実プレー1分あたりの回数に直した13項目，
ボールの持ち方（テンポ）を表す3項目である．
このうち「敵陣3分の1でのプレー率」と「ペナルティエリア進入」の2項目は，
チームがどれだけ前がかりになっているかを直接見るために後から加えたものである．
なお，得点そのものを示すイベントと再開のキックオフのパスは，
答えを直接教えてしまうため特徴量の計算から除外している．

\begin{table}[htbp]
    \centering
    \caption{プレーぶりを表す32項目．いずれも，対象チーム（失点した側，対照ではシュートを受けた側）の視点で実プレー3分間から計算する．}
    \label{tab:features}
    \begin{tabular}{|p{2.8cm}|c|p{10.0cm}|}
    \hline
    分類 & 項目数 & 内訳 \\
    \hline
    内容・位置 & 16 &
    パス成功率，平均パス距離，ロングパス率，浮き球パス率，前向きパス率，クロス率，
    パス位置の高さ，ポゼッション率，平均キャリー距離，前向きキャリー率，
    平均シュート距離，プレス位置の高さ，カウンタープレス率，守備位置の高さ，
    被プレス下プレー率，敵陣3分の1でのプレー率 \\
    \hline
    実プレー1分\newline あたりの回数 & 13 &
    総イベント，パス，キャリー，シュート，xG合計，プレッシャー，守備アクション，
    クリア，ファウル，ドリブル，被プレッシャー，ポゼッション回数，
    ペナルティエリア進入 \\
    \hline
    テンポ & 3 &
    1ポゼッション当たりイベント数，1ポゼッション当たりパス数，
    同一ポゼッション内のイベント間隔 \\
    \hline
    \end{tabular}
\end{table}

\subsection{比較対象（対照）の設定}

失点の直後だけを眺めても，そこで見えた変化が失点によるものなのか，
その時間帯や点差，あるいはそもそも相手に攻め込まれていたことによるものなのかを切り分けられない．
そこで本研究では，相手の決定的な攻撃を受けたところまでは同じで結果だけが違う場面として，
\textbf{枠内シュートをゴールキーパーがセーブした場面}を対照に用いた．
失点した場面（処置）と守り切った場面（対照）を対にして比べれば，
「攻め込まれたこと」の影響は両群に共通するため差し引かれ，
「点が入ってしまったこと」の影響だけが残ることが期待できる．

\subsection{前後の区間のそろえ方}

失点の後は必ずセンターサークルからのキックオフで試合が再開されるため，
失点側の「後の3分」にだけ，キックオフ直後の定型的なプレーが入り込む．
中間発表後の検証で明らかになったとおり，この再開の構造は失点の有無とは無関係に判別を押し上げてしまう．
そこで本研究では，処置群と対照群のどちらについても，
対象チームがその場面の後に最初に行った一続きの攻撃（ポゼッション）が終わった時点を起点とし，
そこから先の実プレー3分間を「後区間」とした（図\ref{fig:design}A）．
「前区間」はその場面の直前の実プレー3分間である．
前後どちらの区間についても，別のゴールが入り込む場面は解析から除外している．

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/fig_design.png}
    \caption{失点場面と対照場面を対にした解析の全体の流れ．A: 処置（失点）と対照（枠内シュートのセーブ）で同じ形に区間を切り出す．どちらも場面の後の最初の一続きの攻撃を測定から外し，そこから先の実プレー3分間を後区間とする．B: 32項目の変化量（後$-$前）を対にしてロジスティック回帰に与え，どちらが失点側かを見分けられるかをAUCで評価する．}
    \label{fig:design}
\end{figure}

\subsection{対応づけ（マッチング）の条件}

処置群の各失点に対し，条件のそろった対照を1つずつ割り当てた．
条件のゆるさを変えた3通りの対応づけを用意し，
条件を厳しくしても同じ結果が得られるかを確かめた（表\ref{tab:matching}）．

広い比較では，同じ前後半・同じ点差状態・同じホーム／アウェーを完全一致の条件とし，
そのうえで時刻差8分以内・xG差0.10以内の中から最も近い相手を選んだ．
xG（得点期待値）とはシュートの位置・角度・体の部位などから計算される得点確率であり，
これをそろえることで「シュートの厳しさ」を対応づけている．

厳しい比較では，これに加えてシュートの種類を完全一致とし，
シュート位置（縦10ヤード・横18ヤード以内），相手チームの順位差，
さらに失点前3分の32項目と，前6〜3分から前3分への変化までを距離に用いて相手を選んだ．
比較するチーム同士の実力差を消すため，順位差3以内のチーム同士で対応づけたものと，
同じチーム内だけで対応づけたものの2通りを用意した．

\begin{table}[htbp]
    \centering
    \caption{3通りの対応づけの条件．厳しい比較では，シュートの条件と失点前のプレーまでそろえる．}
    \label{tab:matching}
    \begin{tabular}{|p{3.0cm}|p{3.5cm}|p{4.0cm}|p{3.5cm}|}
    \hline
    条件 & 広い比較 & 厳しい比較\newline （順位対応） & 厳しい比較\newline （同一チーム） \\
    \hline
    完全一致 & 前後半，点差状態，ホーム／アウェー &
    左の3つ＋シュートの種類 & 左の4つ＋チーム \\
    \hline
    時刻差 & 8分以内 & 12分以内 & 12分以内 \\
    \hline
    xG差 & 0.10以内 & 0.08以内 & 0.08以内 \\
    \hline
    シュート位置 & 条件なし & 縦10・横18ヤード以内 & 縦10・横18ヤード以内 \\
    \hline
    相手の順位差 & 条件なし & 5以内 & 8以内 \\
    \hline
    自チームの順位差 & 条件なし & 3以内 & 同一チーム \\
    \hline
    失点前の状態 & 条件なし &
    前3分の32項目と，前6〜3分からの変化が近いものを優先 &
    順位対応と同じ \\
    \hline
    学習と評価の分離 & 試合を5組に分割 & 試合単位 & チーム単位 \\
    \hline
    \end{tabular}
\end{table}

\subsection{判別モデルと評価の手続き}

場面ごとに後区間と前区間の差（後$-$前）を32項目それぞれについて計算し，これを入力とした．
処置側の変化量と対照側の変化量を対にして与え，
どちらが失点側かをロジスティック回帰に判別させる（図\ref{fig:design}B）．
これは差の差（difference-in-differences）の考え方を判別問題の形にしたものであり，
場面ごとのもともとの水準の違いは，差をとる段階で取り除かれる．

学習と評価の分離には注意を要する．
同じ試合や同じ対が学習側と評価側にまたがると，成績は実際より高く出てしまう．
そこで最終的な解析では，まず全380試合を乱数で5つの組に分け，
対応づけを同じ組の中だけで行い，その組をそのまま交差検証のfoldとして用いた．
この順序であれば，恣意的なパラメータを置くことなく，
同じ試合・同じ対が学習と評価に混ざらないことを設計だけで保証できる．

95\%信頼区間は，対応づけによってつながった試合の塊（連結成分）を単位とするブートストラップ5,000回で求めた．
$p$値は，同じ単位で処置と対照のラベルを入れ替える並べ替え検定5,000回で求めた．

\subsection{事前差の点検}

対応づけが効いているかどうかを確かめるため，同じ手続きで次の2つも判別させた．
(1)~失点前3分の32項目だけを入力したもの，
(2)~前6〜3分から前3分への変化だけを入力したものである．
これらのAUCが0.5付近であれば，両群は失点の直前まで似た状態にあり，
後区間で見えた差は場面の後に生じたものだと言える．
逆にここで判別できてしまう場合は，対応づけの後にも状態の差が残っていることになり，
後区間の差を失点のせいだとは言えない．


\section{実装（実験）}
\label{sec:impl}

\subsection{実験環境}

解析はすべてローカルのノートPC（Apple M5，メモリ32\,GB，macOS 26.5）上で行い，
クラウド環境は使用していない．
実装にはPython 3.12.13を用い，データの整形にpandas 3.0.3とNumPy 2.4.6，
統計処理にSciPy 1.18.0，モデルの学習と評価にscikit-learn 1.9.0，
作図にmatplotlib 3.11.0を使用した．
パッケージの管理にはuvを用い，同じ環境を再現できるようにした．
StatsBombの公開イベントデータはstatsbombpy経由でJSONとして取得し，
リポジトリに保存したうえで解析に用いた．

\subsection{データの規模}

対象は前章で述べたラ・リーガ2015-16シーズンの全380試合であり，含まれるゴールは1,043点である．
このうち，前後の実プレー3分間が確保でき，他のゴールと近接しない「隔離された失点」609件を
処置群の候補とした（313試合に分布する）．
対照群の候補は，同じ380試合から抽出した，枠内シュートをゴールキーパーがセーブした場面である．
対応づけの結果，実際に判別に用いた対の数は，
広い比較でxGまでそろえた場合が285〜291組，xGをそろえない場合が463〜488組，
厳しい比較が66〜79組であった．
1つの対からは処置側・対照側の2件の変化量ベクトルが得られるので，
学習・評価に用いる件数はその2倍である．

\subsection{モデルと代表的なパラメータ}

判別器にはscikit-learnのロジスティック回帰を用いた．
欠測値は学習データの中央値で補完し，各項目を平均0・分散1に標準化したうえで，
正則化パラメータ$C=1.0$，最大反復回数2{,}000で学習した．
交差検証は5分割とし，前章のとおり試合の分割を先に決めてから対応づけを行っている．
乱数のシードは42を基本とし，
分割の仕方によって結論が変わらないことを確かめるため43と44でも同じ解析を行った．
ブートストラップと並べ替え検定はそれぞれ5{,}000回である．

\subsection{評価方法と計算時間}

評価は，交差検証で学習に使っていないfoldに対して出した判定値（out-of-fold予測）を用い，
対になった処置側と対照側のどちらの判定値が高いかでAUCを計算した．
モデルの学習自体は軽く，1回の対応づけから交差検証までは数秒で終わる．
計算時間の大半は，380試合分のイベントJSONから実プレー区間と32項目を復元する前処理が占めており，
最終的な解析（3種類のシード×2種類の対応づけ）の一連の実行にかかった時間は
前述の環境で1分12秒であった．

% 参考文献（第10章担当）へ：本節で挙げた scikit-learn / pandas / NumPy / SciPy /
% matplotlib / uv / statsbombpy を引用できるようにしてもらえると助かります。
% 引用を入れる場合は上の各ライブラリ名の直後に \cite{...} を追加してください。

\subsection{作業の分担}

% TODO: 氏名の当てはめはグループ内で確認してください。
% 根拠にした記録: (1) イベントデータ取得と eventdata.md = GitHub kampachi0096,
% (2) pressures2.py（中間発表のプレッシャー集計）= yaku211,
% (3) リポジトリ/uv環境の整備と初期構築 = KurenNagata,
% (4) 特徴量抽出・モデル・検証・図表 = wisteriahuman。
グループにおける作業の分担は次のとおりである．
＜氏名＞がリポジトリとPython実行環境の整備および進捗の管理を担当し，
＜氏名＞がStatsBomb公開イベントデータの取得とデータ仕様の整理を担当した．
＜氏名＞は中間発表時のプレッシャー回数の集計を担当した．
＜氏名＞は実プレー時間の復元と32項目の特徴量抽出，
対照の設計と対応づけ，判別モデルの学習・評価，および図表の作成を担当した．
発表資料とレポートの各章は担当を分けて分担執筆し，
解析結果の解釈については全員で議論した．
（※この段落はグループ内で記述を共有した）

\section{結果}
\label{sec:result}

\subsection{広い条件で比べたとき}

試合の条件とxGをそろえた広い比較の結果を表\ref{tab:broad}に示す．
xGまでそろえた291組ではAUCが0.628，95\%信頼区間は$[0.581, 0.689]$であり，
下限が0.5を上回った（並べ替え検定 $p = 0.0002$）．
分割のシードを変えても0.639〜0.661であり，
xGをそろえない対応づけでも0.652〜0.663と，同じ水準の値が得られた．
すなわち，この条件では失点側とセーブ側の前後変化を弱いながら見分けられている．

\begin{table}[htbp]
    \centering
    \caption{広い比較の結果．試合を先に5組へ分割してから対応づけを行い（fold先行），学習に使っていないfoldでの判定値からAUCを求めた．95\%信頼区間と$p$値は連結成分を単位に計算した．}
    \label{tab:broad}
    \begin{tabular}{|l|c|c|c|c|}
    \hline
    対応づけ & 分割シード & 対の数 & AUC [95\%信頼区間] & $p$値 \\
    \hline
    xGもそろえる & 42 & 291 & 0.628 [0.581, 0.689] & 0.0002 \\
    \hline
    xGもそろえる & 43 & 290 & 0.661 [0.624, 0.700] & 0.0002 \\
    \hline
    xGもそろえる & 44 & 285 & 0.639 [0.599, 0.682] & 0.0004 \\
    \hline
    基本（xGは条件外） & 42 & 485 & 0.663 [0.625, 0.695] & 0.0172 \\
    \hline
    基本（xGは条件外） & 43 & 463 & 0.663 [0.653, 0.678] & 0.0012 \\
    \hline
    基本（xGは条件外） & 44 & 488 & 0.652 [0.629, 0.681] & 0.0032 \\
    \hline
    \end{tabular}
\end{table}

\subsection{変化の中身}

何が変わっているのかを見るため，32項目それぞれについて，
失点側の前後変化から対照側の前後変化を引いた値（差の差）を求めた．
対の数が確保できた31項目にHolm法による多重比較の補正をかけたところ，
6項目が有意（補正後 $p < 0.05$）となった．
最も明確だったのは\textbf{一続きの攻撃（ポゼッション）の回数が減り，1回を長く持つようになる}という変化で，
1ポゼッション当たりのイベント数が$+2.07$，同パス数が$+0.77$，
実プレー1分当たりのポゼッション回数が$-0.32$回であった．
このほか，平均キャリー距離$-0.63$ヤード，クロス率$+0.83$ポイント，
プレス位置の高さ$+5.06$ヤードが有意となった．

図\ref{fig:slopes}は，このうち代表的な3項目について前後の平均値を結んだものである．
一続きの攻撃の回数はどちらの群でも増えるが，
守り切ったセーブ側の増え方（1.81→2.44回）の方が失点側（1.77→2.08回）より大きい．
1回あたりのパス数は，セーブ側が細切れになる（4.93→4.28本）のに対し，
失点側はわずかに増える（5.04→5.13本）．
プレスを行う位置は失点側の方が大きく前へ出ている（51.4→59.5ヤード）．
なお，ボールを保持していた割合そのものには差が出ていない．
「どれだけ持つか」ではなく「どう持つか」が変わった，という形の差である．

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/fig_feature_slopes.png}
    \caption{広い比較（291組）における失点側と守り切った側の前後平均．A: 一続きの攻撃の回数，B: 1回の攻撃あたりのパス，C: プレスを行った位置．実線が失点した側，破線が守り切った側を表す．3項目とも前3分はほぼ同じ水準にあり，後3分での開き方が異なる．縦軸の範囲は項目ごとに異なる．}
    \label{fig:slopes}
\end{figure}

\subsection{条件を厳しくしたとき}

シュートの位置・種類，相手の順位，さらに失点前のプレーまでそろえた厳しい比較の結果を
表\ref{tab:strict}に示す．
順位をそろえた68組では前後変化のAUCが0.522，
同じチーム内で比べた79組では0.482であり，
どちらも95\%信頼区間が0.5をまたいだ（$p = 0.709$，$p = 0.744$）．
分割のシードを変えても0.482〜0.551の範囲にとどまり，
広い比較で得られた0.63という水準は再現しなかった（図\ref{fig:auc}）．

事前差の点検では，同一チーム内の比較はいずれのシードでも失点前だけのAUCが0.479〜0.512であり，
失点の直前まで両群がよく似た状態にあったことを確認できた．
一方，順位をそろえた比較のシード43では，
前6〜3分から前3分への変化だけでAUCが0.634（$p = 0.002$）と判別できてしまっており，
対応づけの後にも状態の差が残る場合があることが分かった．

個別の項目では，順位をそろえた68組で敵陣3分の1でのプレー率が増える組合せが見られた
（差の差は$+0.074$，95\%信頼区間は0.029〜0.117，Holm補正後の$p$値は0.025）．
ただしこれは，乱数を変えた対応づけと同一チーム内の比較では有意にならず，安定しなかった．

\begin{table}[htbp]
    \centering
    \caption{厳しい比較の結果．「順位対応」はシュートの位置・種類と相手の順位までそろえた比較，「同一チーム」はさらに同じチーム内だけで対応づけた比較である．「失点前」と「事前変化」は対応づけの点検用で，0.5に近いほど両群が失点前まで似た状態にあったことを意味する．括弧内は95\%信頼区間．}
    \label{tab:strict}
    \small
    \begin{tabular}{|l|c|c|c|c|c|}
    \hline
    比較 & シード & 対の数 & 失点前 & 事前変化 & 前後変化 [95\%信頼区間] \\
    \hline
    順位対応 & 42 & 68 & 0.522 & 0.504 & 0.522 [0.414, 0.630] \\
    \hline
    順位対応 & 43 & 75 & 0.574 & 0.634 & 0.535 [0.472, 0.603] \\
    \hline
    順位対応 & 44 & 66 & 0.443 & 0.423 & 0.551 [0.477, 0.629] \\
    \hline
    同一チーム & 42 & 79 & 0.512 & 0.390 & 0.482 [0.383, 0.582] \\
    \hline
    同一チーム & 43 & 79 & 0.495 & 0.390 & 0.494 [0.410, 0.579] \\
    \hline
    同一チーム & 44 & 79 & 0.479 & 0.383 & 0.492 [0.398, 0.594] \\
    \hline
    \end{tabular}
\end{table}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1.0\linewidth]{figures/fig_auc_designs.png}
    \caption{対応づけの条件別にみた失点側とセーブ側の判別成績（いずれも分割シード42）．点はAUCの推定値，横線は95\%信頼区間，破線は0.5（まったく見分けられない水準）を表す．条件を厳しくするほど0.5に近づき，信頼区間はいずれも0.5を含む．}
    \label{fig:auc}
\end{figure}

\section{結論･考察}
＜この章については，400字以上，個々人で記述する場合は氏名を明記の上、ひとり150文字以上．

やったことのまとめ，実験結果から得られた結論等を記述する．苦労した点や今後の展望等あれば見出し付きで記述すると読みやすい．＞



% 参考⽂献
\begin{thebibliography}{99}
% ＜
 \bibitem{ref:1} 著者名，文献のタイトル，文献の出典名（論文誌，国際会議，学会名），（必要に応じ）Vol.をつけて巻，No.をつけて号，pp.をつけて始めのページ-終わりのページ，西暦年．
 \bibitem{ref:2} 著者名，文献のタイトル，文献の出典名（論文誌，国際会議，学会名），（必要に応じ）Vol.をつけて巻，No.をつけて号，pp.をつけて始めのページ-終わりのページ，西暦年．
% ＞
\end{thebibliography}

\clearpage
\input{appendix}

\end{document}
